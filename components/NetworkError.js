import React, { useState } from "react";
import {
  StyleSheet,
  Text,
  Dimensions,
  TextInput,
  SafeAreaView,
  Image,
  View,
  ScrollView,
} from "react-native";

const NetworkError = () => {
  return <View></View>;
};

export default NetworkError;

const styles = StyleSheet.create({});
